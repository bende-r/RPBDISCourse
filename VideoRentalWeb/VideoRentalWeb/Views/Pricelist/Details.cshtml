@model VideoRentalWeb.Models.Entities.PricelistViewModel
@{
    ViewData["Title"] = "Details"; Layout = "_Layout";
}

<div class="container w-50">
    @if (User.Identity.IsAuthenticated)
    {
        <table class="table table-sm">
            <thead class="thead-dark text-center">
                <tr>
                    <th colspan="2">Detailed description of "@Model.Entity.Disk.Title"</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><label asp-for="Entity.DiskId"></label></td>
                    <td>@Model.Entity.DiskId</td>
                </tr>
                <tr>
                    <td><label asp-for="Entity.Disk.Title"></label></td>
                    <td>@Model.Entity.Disk.Title</td>
                </tr>
                <tr>
                    <td><label asp-for="Entity.Disk.CreationYear"></label></td>
                    <td>@Model.Entity.Disk.CreationYear</td>
                </tr>
                <tr>
                    <td><label asp-for="Entity.Disk. MainActor"></label></td>
                    <td>@Model.Entity.Disk.MainActor</td>
                </tr>
                <tr>
                    <td><label asp-for="Entity.Price"></label></td>
                    <td>@Model.Entity.Price.Value</td>
                </tr>
            </tbody>
        </table>
        <p><a asp-action="Index" asp-route-page="@Model.PageViewModel.CurrentPage" class="btn btn-danger w-100">Back</a></p>
    }
    else
    {
        <p>You are not logged in. Please <a href="/Account/Login">login</a></p>
    }
</div>
